<!doctype html>

<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, height=device-height, minimum-scale=1.0, maximum-scale=1.0"
    >

    <!-- Dash.Fi -->
    <script async src="https://pixel.dashfi.dev/static/js/pixel.dash.fi.js"></script>
    <script>
      window.dashFiTracker = window.dashFiTracker || {};
      window.dashFiTracker['siteIdentifier'] = '1f0958b9-91ad-6a5d-9af8-5e43492729ac';
    </script>
    <!-- End Dash.Fi -->
    <!-- Start of Shoplift scripts -->
    {% render 'shoplift' %}
    <!-- End of Shoplift scripts -->

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=Instrument+Sans:ital,wght@0,400..700;1,400..700&display=swap"
      rel="stylesheet"
      media="print"
      onload="this.media='all'"
    >
    <noscript><link href="https://fonts.googleapis.com/css2?family=Instrument+Sans:ital,wght@0,400..700;1,400..700&display=swap" rel="stylesheet"></noscript>

    <link rel="preconnect dns-prefetch" href="https://whale.camera/" crossorigin>
    <script>
/* >> TriplePixel :: start*/
window.TriplePixelData={TripleName:"nmnl-co.myshopify.com",ver:"2.11",plat:"SHOPIFY",isHeadless:false},function(W,H,A,L,E,_,B,N){function O(U,T,P,H,R){void 0===R&&(R=!1),H=new XMLHttpRequest,P?(H.open("POST",U,!0),H.setRequestHeader("Content-Type","application/json")):H.open("GET",U,!0),H.send(JSON.stringify(P||{})),H.onreadystatechange=function(){4===H.readyState&&200===H.status?(R=H.responseText,U.includes(".txt")?eval(R):P||(N[B]=R)):(299<H.status||H.status<200)&&T&&!R&&(R=!0,O(U,T-1,P))}}if(N=window,!N[H+"sn"]){N[H+"sn"]=1,L=function(){return Date.now().toString(36)+"_"+Math.random().toString(36)};try{A.setItem(H,1+(0|A.getItem(H)||0)),(E=JSON.parse(A.getItem(H+"U")||"[]")).push({u:location.href,r:document.referrer,t:Date.now(),id:L()}),A.setItem(H+"U",JSON.stringify(E))}catch(e){}var i,m,p;A.getItem('"!nC`')||(_=A,A=N,A[H]||(E=A[H]=function(t,e,a){return void 0===a&&(a=[]),"State"==t?E.s:(W=L(),(E._q=E._q||[]).push([W,t,e].concat(a)),W)},E.s="Installed",E._q=[],E.ch=W,B="configSecurityConfModel",N[B]=1,O("https://conf.config-security.com/model",5),i=L(),m=A[atob("c2NyZWVu")],_.setItem("di_pmt_wt",i),p={id:i,action:"profile",avatar:_.getItem("auth-security_rand_salt_"),time:m[atob("d2lkdGg=")]+":"+m[atob("aGVpZ2h0")],host:A.TriplePixelData.TripleName,plat:A.TriplePixelData.plat,url:window.location.href,ref:document.referrer,ver:A.TriplePixelData.ver},O("https://api.config-security.com/event",5,p),O("https://whale.camera/live/dot.txt",5)))}}("","TriplePixel",localStorage);
/* << TriplePixel :: end*/
    </script>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, height=device-height, minimum-scale=1.0, maximum-scale=5.0"
    >
    <meta name="theme-color" content="{{ settings.accent_color }}">
    {% render 'social-meta-tags' %}

    <!-- Global site tag (gtag.js) - Google Ads ENHANCED: 796072844 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=AW-796072844"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag('js', new Date());

      gtag('config', 'AW-796072844', { allow_enhanced_conversions: true });
    </script>

    <!-- Google Tag Manager -->
    <script>
      (function (w, d, s, l, i) {
        w[l] = w[l] || [];
        w[l].push({ 'gtm.start': new Date().getTime(), event: 'gtm.js' });
        var f = d.getElementsByTagName(s)[0],
          j = d.createElement(s),
          dl = l != 'dataLayer' ? '&l=' + l : '';
        j.async = true;
        j.src = 'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
        f.parentNode.insertBefore(j, f);
      })(window, document, 'script', 'dataLayer', 'GTM-K87G9PR');
    </script>
    <!-- End Google Tag Manager -->

    {% include 'replo-head.liquid' %}

    <meta
      name="tapcart-banner:appIcon"
      content="https://storage.googleapis.com/tapcart-150607.appspot.com/af8fe44a8dc96ea4b8766897cc1f5364_Untitleddesign12png.png"
      {% include 'booster-seo' %}

    <!-- Cozy Country Redirect Optimzation Code -->
    {{ shop.metafields.CCR.shopat_script }}
    <!-- Cozy Country Redirect Optimzation Code Ends -->

    <link rel="preconnect dns-prefetch" href="https://cdn.shopify.com">

    {% comment %}
      <title>
        {{ page_title }}{% if current_tags %}{% assign meta_tags = current_tags | join: ', ' %} &ndash; {{ 'general.meta.tags' | t: tags: meta_tags }}{% endif %}{% if current_page != 1 %} &ndash; {{ 'general.meta.page' | t: page: current_page }}{% endif %}{% unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless %}
      </title>
    {% endcomment %}

    {%- if page_description -%}
      {% comment %}<meta name="description" content="{{ page_description | escape }}">{% endcomment %}
    {%- endif -%}

    <link rel="canonical" href="{{ canonical_url }}">

    {%- if settings.favicon -%}
      <link rel="shortcut icon" href="{{ settings.favicon | img_url: '96x' }}" type="image/png">
    {%- endif -%}

    {% render 'css-variables' %}
    <style>
      [data-value*='D#'],
      [data-value*='Default'] {
        display: none !important;
      }
    </style>
    <script src="{{ 'jquery-3.5.1.min.js' | asset_url }}" defer></script>

    {{ content_for_header }}

    {%- render 'bold-options-hybrid' -%}
    {%- render 'bold-common' -%}
    {%- render 'sc-includes' -%}
    {% if template == 'product' and product.metafields.inventory.ShappifyHidden or product.type contains '_HIDDEN_' -%}
      <meta name="robots" content="noindex">
    {%- endif %}

    <script>
      {%- if request.page_type == 'policy' -%}
        document.addEventListener('DOMContentLoaded', function() {
          document.querySelector('.shopify-policy__title').classList.add('PageHeader', 'Heading', 'u-h1');
          document.querySelector('.shopify-policy__body').classList.add('Rte');
        });
      {%- endif -%}

      // This allows to expose several variables to the global scope, to be used in scripts
      window.theme = {
        pageType: {{ request.page_type | json }},
        moneyFormat: {{ shop.money_format | json }},
        moneyWithCurrencyFormat: {{ shop.money_with_currency_format | json }},
        currencyCodeEnabled: {{ settings.currency_code_enabled | json }},
        productImageSize: {{ settings.product_image_size | json }},
        searchMode: {{ settings.search_mode | json }},
        showPageTransition: {{ settings.show_page_transition | json }},
        showElementStaggering: {{ settings.show_element_staggering | json }},
        showImageZooming: {{ settings.show_image_zooming | json }}
      };

      window.routes = {
        rootUrl: {{ routes.root_url | json }},
        rootUrlWithoutSlash: {% if routes.root_url == '/' %}''{% else %}{{ routes.root_url | json }}{% endif %},
        cartUrl: {{ routes.cart_url | json }},
        cartAddUrl: {{ routes.cart_add_url | json }},
        cartChangeUrl: {{ routes.cart_change_url | json }},
        searchUrl: {{ routes.search_url | json }},
        productRecommendationsUrl: {{ routes.product_recommendations_url | json }}
      };

      window.languages = {
        cartAddNote: {{ 'cart.general.add_note' | t | json }},
        cartEditNote: {{ 'cart.general.edit_note' | t | json }},
        productImageLoadingError: {{ 'product.slideshow.image_loading_error' | t | json }},
        productFormAddToCart: {% if product.template_suffix == 'pre-order' %}{{ 'product.form.pre_order' | t | json }}{% else %}{{ 'product.form.add_to_cart' | t | json }}{% endif %},
        productFormUnavailable: {{ 'product.form.unavailable' | t | json }},
        productFormSoldOut: {{ 'product.form.sold_out' | t | json }},
        shippingEstimatorOneResult: {{ 'cart.shipping_estimator.one_result_title' | t | json }},
        shippingEstimatorMoreResults: {{ 'cart.shipping_estimator.more_results_title' | t | json }},
        shippingEstimatorNoResults: {{ 'cart.shipping_estimator.no_results_title' | t | json }}
      };

      window.lazySizesConfig = {
        loadHidden: false,
        hFac: 0,
        expFactor: 2,
        ricTimeout: 100,
        lazyClass: 'Image--lazyLoad',
        loadingClass: 'Image--lazyLoading',
        loadedClass: 'Image--lazyLoaded'
      };

      window.cart_minimum = {
        status:{{settings.cart_min_amount_status}},
        min_amount:{{settings.cart_min_amount |json}},
        message:{{settings.min_msg |json}}
      };

      document.documentElement.className = document.documentElement.className.replace('no-js', 'js');
      document.documentElement.style.setProperty('--window-height', window.innerHeight + 'px');

      // We do a quick detection of some features (we could use Modernizr but for so little...)
      (function() {
        document.documentElement.className += ((window.CSS && window.CSS.supports('(position: sticky) or (position: -webkit-sticky)')) ? ' supports-sticky' : ' no-supports-sticky');
        document.documentElement.className += (window.matchMedia('(-moz-touch-enabled: 1), (hover: none)')).matches ? ' no-supports-hover' : ' supports-hover';
      }());

      {% if request.page_type == 'captcha' %}
        setTimeout(function() {
          window.scrollTo(0,0);
        }, 250)
      {% endif %}
    </script>


    
    

    <link href="{{ 'theme.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
    <link href="{{ 'custom.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
    <link href="{{ 'nominal.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
    <link href="{{ 'slick.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
    <link href="{{ 'a11y.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
    <link href="{{ 'nominal-custom.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
    <link rel="stylesheet" href="{{ 'test-implementation.css' | asset_url }}" media="print" onload="this.media='all'">
    <noscript><link rel="stylesheet" href="{{ 'test-implementation.css' | asset_url }}"></noscript>
    {{ 'custom-new.css' | asset_url | stylesheet_tag }}
    {{ 'style.css' | asset_url | stylesheet_tag }}
    {%- if template == 'product' -%}
      {{ 'ab26-description.css' | asset_url | stylesheet_tag }}
    {%- endif -%}
    {% if localization.country.iso_code == 'US' or localization.country.iso_code == 'CA' %}<link rel="stylesheet" href="{{ 'ab25-cart.css' | asset_url }}" media="print" onload="this.media='all'"><noscript><link rel="stylesheet" href="{{ 'ab25-cart.css' | asset_url }}"></noscript>{% endif %}

    <script src="{{ 'lazysizes.min.js' | asset_url }}" async></script>

    {%- if template == 'customers/addresses' -%}
      <script src="{{ 'shopify_common.js' | shopify_asset_url }}" defer></script>
      <script src="{{ 'customer_area.js' | shopify_asset_url }}" defer></script>
    {%- endif -%}
    <script src="{{ 'libs.min.js' | asset_url }}" defer></script>
    <script src="{{ 'theme.js' | asset_url }}" defer></script>
    <script src="{{ 'slick.min.js' | asset_url }}" defer></script>
    <script src="{{ 'custom.js' | asset_url }}" defer></script>
    <script src="{{ 'custom-new.js' | asset_url }}" defer></script>
    <script src="{{ 'ab26-description.js' | asset_url }}" defer></script>

    <script>
      (function () {
        window.onpageshow = function () {
          if (window.theme.showPageTransition) {
            var pageTransition = document.querySelector('.PageTransition');

            if (pageTransition) {
              pageTransition.style.visibility = 'visible';
              pageTransition.style.opacity = '0';
            }
          }

          // When the page is loaded from the cache, we have to reload the cart content
          /*document.documentElement.dispatchEvent(new CustomEvent('cart:refresh', {
            bubbles: true
          }));*/
        };
      })();
    </script>

    {% render 'microdata-schema' %}

    {% if template == 'page.okendo-reviews' %}
      {{ 'okendo-dedicated-reviews.js' | asset_url | script_tag }}
    {% endif %}

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-TKCSXQNY4S"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag('js', new Date());

      gtag('config', 'G-TKCSXQNY4S');
    </script>

    {% render 'shogun-head' %}

    {% render 'delivery_coder' %}
    {%- if template contains 'product' -%}
      <link rel="preload" href="{{ 'design-pack-styles.css' | asset_url }}" as="style">
      {{ 'design-pack-styles.css' | asset_url | stylesheet_tag }}
    {%- endif -%}

    <!-- Varify.io® code for Nominal -->

    <script>
      window.varify = window.varify || {};
      window.varify.iid = 4433;
    </script>
    <script src="https://app.varify.io/varify.js"></script>
    <style>
      shopify-payment-terms {
        display: none !important;
      }
      .ProductForm__QuantitySelector {
        display: none !important;
      }
      .swym-button-bar {
        display: none !important;
      }
    </style>
  </head>

  {%- capture classes -%}features--heading-{{ settings.heading_size }}{%- endcapture -%}

  {%- if settings.uppercase_heading -%}
    {%- assign classes = classes | append: ' features--heading-uppercase' -%}
  {%- endif -%}

  {%- if settings.product_show_price_on_hover -%}
    {%- assign classes = classes | append: ' features--show-price-on-hover' -%}
  {%- endif -%}

  {%- if settings.show_page_transition -%}
    {%- assign classes = classes | append: ' features--show-page-transition' -%}
  {%- endif -%}

  {%- if settings.show_button_transition -%}
    {%- assign classes = classes | append: ' features--show-button-transition' -%}
  {%- endif -%}

  {%- if settings.show_image_zooming -%}
    {%- assign classes = classes | append: ' features--show-image-zooming' -%}
  {%- endif -%}

  {%- if settings.show_element_staggering -%}
    {%- assign classes = classes | append: ' features--show-element-staggering' -%}
  {%- endif -%}

  <body class="prestige--v4 {{ classes }} {% if template.directory %}template-{{ template.directory | handle }}{% endif %} template-{{ template.name | handle }}">
    <!-- Google Tag Manager (noscript) -->
    <noscript
      ><iframe
        src="https://www.googletagmanager.com/ns.html?id=GTM-K87G9PR"
        height="0"
        width="0"
        style="display:none;visibility:hidden"
      ></iframe
    ></noscript>
    <!-- End Google Tag Manager (noscript) -->

    <!-- SLIDE CART EDITS - START -->
    <style>
      #slidecarthq .header {
        padding: 5px 10px !important;
      }
      #slidecarthq > div.slidecarthq.right.light.open.cartOne > div.rewards > div.rewards-unlock-text > div > p > span {
        font-size: 12.75px !important;
      }
      #slidecarthq .rewards {
        gap: 0px !important;
      }
      #slidecarthq .slidecarthq > * {
        flex-shrink: 0;
      }
      #slidecarthq .rewards {
        padding: 5px 30px !important;
      }
      #slidecarthq
        > div.slidecarthq.right.light.open.cartOne
        > div.rewards
        > div:nth-child(2)
        > div.rewards-tiers-labels
        > div.rewards-tiers-container.rewards-tiers-container-no-icons
        > div.rewards-tiers-labels-item {
        display: none !important;
      }
      #slidecarthq .rewards p {
        text-align: center !important;
      }
      .sticky-checkout {
        margin-top: auto;
        position: sticky;
        bottom: 0;
        padding: 20px;
        background-color: #f2f2f2 !important;
        border-top: 1px solid rgba(0, 0, 0, 0.05);
        z-index: 99;
      }
      .no-sticky-checkout {
        margin-top: unset !important;
      }
      @media (min-width: 440px) {
        #slidecarthq .footer.new-footer {
          padding: 20px 30px 0;
          position: sticky !important;
        }
      }
      #slidecarthq > div.slidecarthq.right.light.open > footer {
        position: sticky !important;
      }
      #slidecarthq .announcements p strong,
      #slidecarthq .announcements p a {
        font-size: 12.5px !important;
      }
      @media only screen and (max-width: 600px) {
        #slidecarthq .rewards-tiers-labels {
          display: flex;
          flex-direction: row;
        }
        #slidecarthq .rewards-tiers-labels > .rewards-tiers-labels-item,
        #slidecarthq .rewards-tiers-labels > .rewards-tiers-labels-item:last-child {
          display: block;
          max-width: 29%;
          text-align: right;
          transform: none;
          margin-left: 50px;
        }
        #slidecarthq .header {
          padding: 5px 30px !important;
        }
      }
      #slidecarthq:has(.footer-sticky) footer.new-footer {
        position: sticky;
        z-index: 5;
        background: #f2f2f2;
      }
      #slidecarthq .upsells {
        padding: 5px 15px;
        margin-bottom: 20px;
      }
      #slidecarthq .footer-sticky {
        padding: 5px 30px !important;
      }
      .slidecarthq .additional-buttons li:has(div:not([data-testid='ApplePay-button'])),
      .slidecarthq .additional-buttons li:has(form:not([data-testid='ApplePay-button'])),
      .slidecarthq .additional-buttons li:has(iframe:not([data-testid='ApplePay-button'])) {
        display: none !important;
      }
      .slidecarthq .additional-buttons ul,
      .slidecarthq .additional-buttons li {
        display: block !important;
        width: 100% !important;
      }
      #slidecarthq .additional-buttons .shopify-cleanslate ul li {
        width: 100% !important;
      }
      #slidecarthq .item {
        padding: 10px 20px;
      }
      #slidecarthq .item img {
        width: 80px;
        height: 80px;
      }
      #slidecarthq .item .main-top {
        margin: 0;
      }
      #slidecarthq .item[data-line-item-id='43542369534139'] .quantity-selector * {
        display: none;
      }
      #slidecarthq .free-gifts {
        padding: 20px 0 10px !important;
      }
      #slidecarthq .free-gifts-item {
        height: 85% !important;
        justify-content: center !important;
        align-items: center !important;
      }
      #slidecarthq .free-gifts-item-media .media-wrapper {
        height: 70px !important;
        width: 80px !important;
      }
      #slidecarthq
        > div.slidecarthq.right.light.open
        > section
        > div.free-gifts-body
        > div
        > div
        > div
        > div
        > div
        > div
        > div
        > div.free-gifts-item-body
        > h3 {
        font-size: 14.4px !important;
        line-height: 18px !important;
      }
      #slidecarthq
        > div.slidecarthq.right.light.open
        > section
        > div.free-gifts-body
        > div
        > div
        > div
        > div
        > div
        > div
        > div
        > div.free-gifts-item-body {
        padding: 10px 5px 10px 5px !important;
      }
      #slidecarthq > div.slidecarthq.right.light.open > section > div.free-gifts-body {
        height: 230px !important;
      }
      #slidecarthq .free-gifts .slick-wrapper {
        padding: 0 0px !important;
      }
      @media only screen and (max-width: 700px) {
        #slidecarthq .additional-buttons .shopify-cleanslate ul {
          flex-direction: unset !important;
          flex-wrap: wrap !important;
        }
        #slidecarthq .additional-buttons .shopify-cleanslate ul li {
          padding: 0 2px 0 2px !important;
        }
        #slidecarthq .header {
          padding: 5px 30px !important;
        }
        #slidecarthq .item {
          padding: 10px 14px;
        }
        #slidecarthq .upsells .upsell-image img,
        #slidecarthq .item img {
          width: 60px;
          height: 60px;
        }
        #slidecarthq .quantity-selector button {
          width: 25px;
          height: 20px;
        }
        #slidecarthq .quantity-selector input {
          height: 25px !important;
          width: 40px !important;
        }
        #slidecarthq .item .item-container {
          gap: 10px;
        }
      }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/axios@0.21.1/dist/axios.min.js" defer></script>
    <script>
      let scAnnouncementsInterval;
      var noteText = "Partial Shipment Requested";
      var slidecartUS = false;
      var slidecartHasBoth = false;
    
      function scChangeNextAnnouncement() {
        document.querySelector(".slidecarthq .slick-next")?.click();
      }
    
      function scToggleStickyCheckout () {
        const scItems = document.querySelector('#slidecarthq .items');
        
        if (scItems) {
          let items_height = scItems.clientHeight;
    
          if (items_height >= (window.innerHeight/2)) {
            document.querySelector('#slidecarthq .footer').classList.add('sticky-checkout');
            document.querySelector('#slidecarthq .footer').classList.remove ('no-sticky-checkout');
          } else {
            document.querySelector('#slidecarthq .footer').classList.remove ('sticky-checkout');
            document.querySelector('#slidecarthq .footer').classList.add ('no-sticky-checkout');
          }
        }
      }
    
      window.SLIDECART_OPENED = () => {
        scAnnouncementsInterval = setInterval(scChangeNextAnnouncement, 3000);
        scToggleStickyCheckout();
      }
    
      window.SLIDECART_CLOSED = () => {
        clearInterval(scAnnouncementsInterval);
      }
    
      function scAdjustSubtotalPosition() {
        let scFooter = document.querySelector("#slidecarthq .footer-sticky");
        let scSubtotalRow = document.querySelector("#slidecarthq footer.new-footer");
        
        if (scFooter && scSubtotalRow) {
          let footerHeight = `${scFooter.offsetHeight}px`;
        
          scSubtotalRow.style.bottom = footerHeight;
        }
      }
    
      function formatMoney(cents, format) {
        if (typeof cents === 'string') {
          cents = cents.replace('.', '');
        }
    
        var placeholderRegex = /\{\{\s*(\w+)\s*\}\}/,
            formatString = format || '${{amount}}';
    
            function defaultTo(value, defaultValue) {
              return value == null || value !== value ? defaultValue : value;
            }
    
        function formatWithDelimiters(number, precision, thousands, decimal) {
          precision = defaultTo(precision, 2);
          thousands = defaultTo(thousands, ',');
          decimal = defaultTo(decimal, '.');
    
          if (isNaN(number) || number == null) {
            return 0;
          }
    
          number = (number / 100.0).toFixed(precision);
          var parts = number.split('.'),
              dollarsAmount = parts[0].replace(/(\d)(?=(\d\d\d)+(?!\d))/g, '$1' + thousands),
              centsAmount = parts[1] ? decimal + parts[1] : '';
          return dollarsAmount + centsAmount;
        }
    
        var value = '';
    
        switch (formatString.match(placeholderRegex)[1]) {
          case 'amount':
            value = formatWithDelimiters(cents, 2);
            break;
    
          case 'amount_no_decimals':
            value = formatWithDelimiters(cents, 0);
            break;
    
          case 'amount_with_space_separator':
            value = formatWithDelimiters(cents, 2, ' ', '.');
            break;
    
          case 'amount_no_decimals_with_comma_separator':
            value = formatWithDelimiters(cents, 0, ',', '.');
            break;
    
          case 'amount_no_decimals_with_space_separator':
            value = formatWithDelimiters(cents, 0, ' ');
            break;
    
          case 'amount_with_comma_separator':
            value = formatWithDelimiters(cents, 2, '.', ',');
            break;
        }
    
        if (formatString.indexOf('with_comma_separator') !== -1) {
          return formatString.replace(placeholderRegex, value).replace(',00', '');
        } else {
          return formatString.replace(placeholderRegex, value).replace('.00', '');
        }
      }
    
      function scToggleMinimumCheckout(cart) {
        if (cart.item_count > 0) {
          if (window.cart_minimum && window.cart_minimum.status == true) {
            var amt = window.cart_minimum.min_amount;
            amt = amt * window.Shopify.currency.rate;
            if (amt > cart.total_price) {
              var cartmsg = window.cart_minimum.message;
              var minAmount = formatMoney(amt, window.theme.moneyFormat);
              cartmsg = cartmsg.replace("{amount}", minAmount);
              
              $("<span class='cart-min-msg'>"+cartmsg+"</span>").insertBefore($(".slidecarthq .announcements"));
              $(".footer-sticky .footer-buttons").hide();
              $(".cart-page-msg").html("<span class='cart-min-msg'>"+cartmsg+"</span>");
            } else {
              $(".footer-sticky .footer-buttons").show();
              $(".cart-min-msg").remove();
            }
          }
        } else {
          $(".footer-sticky .footer-buttons").show();
          $(".cart-min-msg").remove();
        }
      }
    
      function scCheckPartialShipping (cart) {
        var custom = false;
        var noncustom = false;
    
        cart.items.forEach(function(item) {
          if (item.product_id != 1806632681515) {
            if (item.title.toLowerCase().indexOf('custom') > -1) {
              custom = true;
            } else {
              noncustom = true;
            }
          }
        })
    
        if (custom && noncustom) {
          slidecartHasBoth = true;
        } else {
          slidecartHasBoth = false;
        }
    
        // Display if in us and app loaded
        var el = document.querySelector('.slidecart-partial');
    
        if (slidecartUS && slidecartHasBoth) {
          if (el) el.classList.add('active');
    
          // Check if note is present      
          if (cart.attributes.partial_shipping == "true" && el) {
            // console.log('in herrr')
            el.querySelector('input').checked = true;
          }
        } else {
          if (el) el.classList.remove('active')
    
          if (cart.attributes.partial_shipping == "true") {
            axios.post('/cart/update.js', { attributes: { partial_shipping: false }})
          }
        }
      }
    
      {% raw %}
      const scFreeShippingConfig = {
        'USD': {
          amount: '75',
          label: "Free US Shipping",
          pre_unlock_text: "You're {{ amount }} away from FREE US Shipping",
          post_unlock_text: "Mabrouk! US Shipping is on us!"
        },
        'CAD': {
          amount: '150',
          label: "Free CA Shipping",
          pre_unlock_text: "You're {{ amount }} away from FREE CA Shipping",
          post_unlock_text: "Mabrouk! CA Shipping is on us!"
        },
        'GBP': {
          amount: '15',
          label: "Free UK Shipping",
          pre_unlock_text: "You're {{ amount }} away from FREE UK Shipping",
          post_unlock_text: "Mabrouk! UK Shipping is on us!"
        }
      };
      {% endraw %}
    
      function scConvertFreeShippingThreshold () {
        const currencyActive = window.Shopify.currency.active;
        
        if (currencyActive in scFreeShippingConfig) {
          // use specified threshold if set
          window.SLIDECART_STATE().settings.rewards_tiers.forEach((reward) => {
            if (reward.rewards_type === 'free_shipping') {
              const localConfig = scFreeShippingConfig[currencyActive];
              reward.amount = localConfig.amount;
              reward.label = localConfig.label;
              reward.pre_unlock_text = localConfig.pre_unlock_text;
              reward.post_unlock_text = localConfig.post_unlock_text;
            }
          });
        } else {
          // disable slide cart rewards
          window.SLIDECART_STATE().settings.rewards = false;
        }
      }
    
      function scAddListeners () {
        document.addEventListener('change', function(e) {
          if (e.target.matches('.slidecart-partial input')) {
            e.preventDefault();
            
            var el = document.querySelector('.slidecart-partial');
            
            if (e.target.checked) {
              axios.post('/cart/update.js', { attributes: { partial_shipping: true }})        
            } else {
              axios.post('/cart/update.js', { attributes: { partial_shipping: false }})
            }
          }
        }, false);
      }
    
      window.SLIDECART_LOADED = cart => {
        scAddListeners();
        
        if (window.SLIDECART_STATE().settings.rewards) {
          scConvertFreeShippingThreshold();
          setTimeout(window.SLIDECART_UPDATE, 500);
        }
      }
    
      window.SLIDECART_UPDATED = async cart => {
        // show compare-at prices start
        try {
          var newCart = cart;
          var total = 0;
    
          for(var i = 0; i < newCart.items.length; i++) {
            var item = newCart.items[i];
            var res = await axios.get('/products/' + item.handle + '.js');
            var variant = res.data.variants.find(i => i.id == item.variant_id);
            var compare = variant.compare_at_price;
            total += (compare - variant.price) * item.quantity;
            newCart.items[i].original_line_price = compare * item.quantity;
          }
    
          newCart.total_discount = total;
    
          window.SLIDECART_SET_CART(newCart);
        } catch (e) {
          console.log('Slide Cart Error: ', e);
        }
        // show compare-at prices end
        
        scAdjustSubtotalPosition();
        setTimeout(scAdjustSubtotalPosition, 1500);
    
        fetch(window.Shopify.routes.root+'cart.js').then(res => res.json()).then((cart) => {
          scToggleMinimumCheckout(cart);
          scCheckPartialShipping(cart);
        });
      }
    </script>
    <!-- SLIDE CART EDITS - END -->

    {% include 'okendo-svg' %}
    {%- if template.name == 'product' or template.name == 'cart' -%}
    <script>
      window.KlarnaThemeGlobals={};{%case template.name%} {% when "product" %} window.KlarnaThemeGlobals.data_purchase_amount={{product.first_available_variant.price}};window.KlarnaThemeGlobals.productVariants={{product.variants|json}};window.KlarnaThemeGlobals.documentCopy=document.cloneNode(true);{% when "cart" %} window.KlarnaThemeGlobals.data_purchase_amount={{cart.total_price}};window.KlarnaThemeGlobals.documentCopy=document.cloneNode(true);{% endcase %}
    </script>
    {%- endif -%}
    <!-- Adobe fonts (lazy-loaded via print/onload to avoid render-blocking) -->
    <link rel="stylesheet" href="https://use.typekit.net/nnd4iyk.css" media="print" onload="this.media='all'">
    <noscript><link rel="stylesheet" href="https://use.typekit.net/nnd4iyk.css"></noscript>

    {%- comment -%}Common SVG definitions that we are re-using in several places{%- endcomment -%}
    <svg class="u-visually-hidden">
      <linearGradient id="rating-star-gradient-half">
        <stop offset="50%" stop-color="var(--product-star-rating)" />
        <stop offset="50%" stop-color="var(--text-color-light)" />
      </linearGradient>
    </svg>

    <a class="PageSkipLink u-visually-hidden" href="#main">{{ 'header.navigation.skip_to_content' | t }}</a>
    <span class="LoadingBar"></span>
    <div class="PageOverlay"></div>

    {%- if settings.show_page_transition -%}
      <div class="PageTransition"></div>
    {%- endif -%}

    {% section 'popup' %}
    {% section 'sidebar-menu' %}

    {%- if template != 'cart' -%}
      {% render 'cart-drawer' %}
    {%- endif -%}

    <div class="PageContainer">
      {% section 'announcement' %}
      {% section 'header' %}

      <main id="main" role="main">
        {{ content_for_layout }}
      </main>

      {% section 'footer' %}
    </div>
    <script async type="text/javascript" src="//static.klaviyo.com/onsite/js/klaviyo.js?company_id=Np2n2K"></script>
    {% if customer %}
      <script>
        var _learnq = _learnq || [];
        if ('{{ customer.email }}') {
          _learnq.push([
            'identify',
            {
              $email: '{{ customer.email }}',
            },
          ]);
        }
      </script>
    {% endif %}
    {% if template contains 'product' %}
      <!-- <script src="https://a.klaviyo.com/media/js/onsite/onsite.js"></script> -->
      <!--
        <script>
          var klaviyo = klaviyo || [];
          klaviyo.init({
            account: "Np2n2K",
            platform: "shopify"
          });
          klaviyo.enable("backinstock",{
            trigger: {
              product_page_text: "Notify Me When Available",
              product_page_class: "btn",
              product_page_text_align: "center",
              product_page_margin: "0px",
              replace_anchor: false
            },
            modal: {
              headline: "{product_name}",
              body_content: "Register to receive a notification when this item comes back in stock.",
              email_field_label: "Email",
              button_label: "Notify me when available",
              subscription_success_label: "You're in! We'll let you know when it's back.",
              footer_content: '',
              additional_styles: "@import url('https://fonts.googleapis.com/css?family=Helvetica+Neue');",
              drop_background_color: "#000",
              background_color: "#fff",
              text_color: "#222",
              button_text_color: "#fff",
              button_background_color: "#439fdb",
              close_button_color: "#ccc",
              error_background_color: "#fcd6d7",
              error_text_color: "#C72E2F",
              success_background_color: "#d3efcd",
              success_text_color: "#1B9500"
            }
          });
        </script>
      -->
    {% endif %}
    {% include 'smile-initializer' %}

    <!-- **BEGIN** Hextom USB Integration // Main Include - DO NOT MODIFY -->
    {% render 'hextom_usb_main', product: product, customer: customer %}
    <!-- **END** Hextom USB Integration // Main Include - DO NOT MODIFY -->

    {% include 'swymSnippet' %}

    <!-- Gorgias Chat Widget Start -->
    <script
      id="gorgias-chat-widget-install-v3"
      src="https://config.gorgias.chat/bundle-loader/01GYCC9J042H8CRXRFMNY971M8?source=shopify1click"
    ></script>
    <script id="gorgias-chat-shopify-install">
      !(function (_) {
        (_.SHOPIFY_PERMANENT_DOMAIN = '{{shop.permanent_domain}}'),
          (_.SHOPIFY_CUSTOMER_ID = '{{customer.id}}'),
          (_.SHOPIFY_CUSTOMER_EMAIL = '{{customer.email}}');
      })(window || {});
    </script>
    <!-- Gorgias Chat Widget End -->
    {% if template contains 'product' %}
      {% if product.handle == 'nominal-premium-vip-pass' %}
        <style>
          .slidecart-form-error {
            display: none !important;
          }
        </style>
      {% endif %}
    {% endif %}
    <style>
      @font-face {
        font-family: 'GT-Helvetica';
        src: url('{{ "GT-Helvetica.woff2" | asset_url }}') format('woff2'),
          url('{{ "GT-Helvetica.woff" | asset_url }}') format('woff');
            font-weight: normal;
            font-style: normal;
            font-display: swap;
            }
      .Linklist__Item{
        margin-bottom:5px;
      }
      span.tag-content {
        min-width: 25px;
        text-align: center;
        line-height: normal;
        padding: 3px 4px;
      }
      .HorizontalList__Item a.Heading{
        font-family: "Instrument Sans";
        font-weight: 600;
        font-size: 13px;
        color: #0d0d0d;
        opacity: .7;
        letter-spacing: 1px;
      }
      .Linklist .Linklist__Item .mega-menu__linklist-link{
        opacity:0.7;
      }
      .HorizontalList__Item a.Heading:hover, .Linklist .Linklist__Item .mega-menu__linklist-link:hover{
        opacity:1;
      }
      .mega-menu__linklist .menu__heading{
        font-family:Inter,Helvetica,sans-serif;
        font-weight:800;
        font-size: 13px;
      }
      .Linklist .Linklist__Item .mega-menu__linklist-link{
        font-family: "Instrument Sans";
        font-size: 12px;
        color:#0d0d0d;
      }
      .Linklist .Linklist__Item .mega-menu__linklist-link span.mega-menu-item{
        transition-duration: .15s;
        transition-property: all;
        transition-timing-function: cubic-bezier(.4,0,.2,1);
        position:relative;
      }
      .Linklist .Linklist__Item .mega-menu__linklist-link span.mega-menu-item:after{
        background:currentColor;
        border-radius:3rem;
        bottom:0;
        content:"";
        height:1px;
        left:0;
        position:absolute;
        transform:scaleX(0);
        transform-origin:bottom right;
        transition:transform 1s cubic-bezier(.19,1,.22,1);
        width:100%
      }
      .Linklist .Linklist__Item .mega-menu__linklist-link span.mega-menu-item:active:after{
        transform:scaleX(1);
        transform-origin:bottom left
      }
      @media (hover:hover) and (pointer:fine){
        .Linklist .Linklist__Item .mega-menu__linklist-link span.mega-menu-item:hover:after{
          transform:scaleX(1);
          transform-origin:bottom left
        }
      }
      .Linklist .Linklist__Item .mega-menu__linklist-link span.mega-menu-item:hover{
        opacity: 1;
        transform: scaleX(1);
        transform-origin: bottom left;
      }
    </style>

    {% if product %}
      <script type="text/javascript">
        var _learnq = _learnq || [];
        var classname = document.getElementsByClassName('ProductForm__AddToCart');
        var addToCart = function () {
          _learnq.push(['track', 'Added to Cart Klaviyo Default', item]);
        };
        for (var i = 0; i < classname.length; i++) {
          classname[i].addEventListener('click', addToCart, false);
        }
      </script>
    {% endif %}

    <script src="https://unpkg.com/@loophq/onstore-sdk@1.5.14/dist/loop-onstore-sdk.js" defer></script>
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        if (typeof LoopOnstore !== 'undefined') {
          LoopOnstore.init({
            key: 'f61c0180c042477956d60ae9b2b7f46af8844fd4',
            attach: '.Cart__Checkout',
          });
        }
      });
    </script>

    <script src="//cdn.shopify.com/s/files/1/0763/7978/3469/t/1/assets/gem.js" type="text/javascript"></script>

    <script
      src="https://nmnl-co.myshopify.com/apps/delivery/delivery_coder.js?t=1689376709.4313&n=736712391"
      defer
    ></script>
    <script src="{{ 'a11y.js' | asset_url }}" defer></script>

    <style>
      @media (max-width: 767px) {

         {% comment %} .nav-tab-contents .Collapsible.tab-content-wrapper {
           column-count: 1 !important;
         } {% endcomment %}

         .SidebarMenu_Primary_content .Collapsible__Inner {
             border: none !important;
             padding: 0 !important;
             border-radius: 0 !important;
         }

       }
    </style>
    {%- capture ocu_extend -%}{% render 'oneclickupsellapp-extend' %}{%- endcapture -%}
    {%- unless ocu_extend contains 'Liquid error' -%}{{ ocu_extend }}{%- endunless -%}
    <script src="{{ 'test-implementation.js' | asset_url }}" defer></script>
    {% if localization.country.iso_code == 'US' or localization.country.iso_code == 'CA' %}<script src="{{ 'ab25-cart.js' | asset_url }}" defer></script>{% endif %}
    <script src="{{ 'ab17.js' | asset_url }}" defer></script>
    <script>
      setTimeout(function() {
          var script = document.createElement('script');
          script.src = '{{ 'ab19.js' | asset_url }}';
          script.async = true;
          document.body.appendChild(script);
      }, 1000);
    </script>
  </body>
</html>
